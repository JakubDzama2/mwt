import { LudiaToStringPipe } from './ludia-to-string.pipe';

describe('LudiaToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new LudiaToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
